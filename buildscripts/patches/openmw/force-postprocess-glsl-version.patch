diff --git a/apps/openmw/mwrender/postprocessor.cpp b/apps/openmw/mwrender/postprocessor.cpp
index ff9ac2a42e..7ff839c358 100644
--- a/apps/openmw/mwrender/postprocessor.cpp
+++ b/apps/openmw/mwrender/postprocessor.cpp
@@ -194,18 +194,20 @@ namespace MWRender
 
         if (!ext->glDisablei && ext->glDisableIndexedEXT)
             ext->glDisablei = ext->glDisableIndexedEXT;
-
+/*
 #ifdef ANDROID
         ext->glDisablei = nullptr;
 #endif
-
+*/
         if (ext->glDisablei)
             mNormalsSupported = true;
         else
             Log(Debug::Error) << "'glDisablei' unsupported, pass normals will not be available to shaders.";
 
-        mGLSLVersion = static_cast<int>(ext->glslLanguageVersion * 100);
-        mUBO = ext->isUniformBufferObjectSupported && mGLSLVersion >= 330;
+//        mGLSLVersion = static_cast<int>(ext->glslLanguageVersion * 100);
+//        mUBO = ext->isUniformBufferObjectSupported && mGLSLVersion >= 330;
+        mGLSLVersion = (getenv("OPENMW_FORCEGLSL330") == nullptr) ? 120 : 330;
+        mUBO = (getenv("OPENMW_UBO") == nullptr) ? false : true;
         mStateUpdater = new Fx::StateUpdater(mUBO);
 
         addChild(mHUDCamera);
diff --git a/components/fx/pass.cpp b/components/fx/pass.cpp
index eceffa188f..e4b3ae61fd 100644
--- a/components/fx/pass.cpp
+++ b/components/fx/pass.cpp
@@ -63,6 +63,7 @@ namespace Fx
     std::string Pass::getPassHeader(Technique& technique, std::string_view preamble, bool fragOut)
     {
         std::string header = R"GLSL(
+// OMW Post-Process shader
 #version @version @profile
 @extensions
 
@@ -85,6 +86,8 @@ namespace Fx
 #define omw_Texture3D @texture3D
 #define omw_Vertex @vertex
 #define omw_FragColor @fragColor
+#define texture2D texture
+#define texture3D texture
 
 @fragBinding
 
diff --git a/components/fx/technique.cpp b/components/fx/technique.cpp
index 945a3cf1b1..7f316d42b8 100644
--- a/components/fx/technique.cpp
+++ b/components/fx/technique.cpp
@@ -84,7 +84,7 @@ namespace Fx
         mDescription = {};
         mVersion = {};
         mGLSLExtensions.clear();
-        mGLSLVersion = (mUBO || Stereo::getMultiview()) ? 330 : 120;
+        mGLSLVersion = (getenv("OPENMW_FORCEGLSL330") == nullptr) ? 120 : 330;//(mUBO || Stereo::getMultiview()) ? 330 : 120;
         mGLSLProfile.clear();
         mDynamic = false;
     }
@@ -241,7 +241,8 @@ namespace Fx
             else if (key == "author")
                 mAuthor = parseString();
             else if (key == "glsl_version")
-                mGLSLVersion = std::max(mGLSLVersion, parseInteger());
+                parseInteger();
+                //mGLSLVersion = std::max(mGLSLVersion, parseInteger());
             else if (key == "flags")
                 mFlags = parseFlags();
             else if (key == "hdr")
@@ -364,7 +365,7 @@ namespace Fx
         if (!pass)
             pass = std::make_shared<Fx::Pass>();
 
-        pass->mUBO = mUBO;
+        pass->mUBO = (getenv("OPENMW_UBO") == nullptr) ? false : true;//mUBO;
         pass->mName = mBlockName;
 
         if (pass->mCompute)
