diff --git a/src/osg/AlphaFunc.cpp b/src/osg/AlphaFunc.cpp
index 0cdd898d6..0b50bb50b 100644
--- a/src/osg/AlphaFunc.cpp
+++ b/src/osg/AlphaFunc.cpp
@@ -32,7 +32,7 @@ void AlphaFunc::apply(State&) const
 #ifdef OSG_GL_FIXED_FUNCTION_AVAILABLE
     glAlphaFunc((GLenum)_comparisonFunc,_referenceValue);
 #else
-    OSG_NOTICE<<"Warning: AlphaFunc::apply(State&) - not supported."<<std::endl;
+    //OSG_NOTICE<<"Warning: AlphaFunc::apply(State&) - not supported."<<std::endl;
 #endif
 }
 
diff --git a/src/osg/ClipPlane.cpp b/src/osg/ClipPlane.cpp
index 3a3fd8567..5240ee837 100644
--- a/src/osg/ClipPlane.cpp
+++ b/src/osg/ClipPlane.cpp
@@ -46,7 +46,7 @@ void ClipPlane::apply(State&) const
 #if defined(OSG_GL_FIXED_FUNCTION_AVAILABLE) && !defined(OSG_GLES1_AVAILABLE)
     glClipPlane((GLenum)(GL_CLIP_PLANE0+_clipPlaneNum),_clipPlane.ptr());
 #else
-    OSG_NOTICE<<"Warning: ClipPlane::apply(State&) - not supported."<<std::endl;
+    //OSG_NOTICE<<"Warning: ClipPlane::apply(State&) - not supported."<<std::endl;
 #endif
 }
 
diff --git a/src/osg/Fog.cpp b/src/osg/Fog.cpp
index 1eb065840..448d0d3a4 100644
--- a/src/osg/Fog.cpp
+++ b/src/osg/Fog.cpp
@@ -68,6 +68,6 @@ void Fog::apply(State& state) const
     }
 
 #else
-    OSG_NOTICE<<"Warning: Fog::apply(State&) - not supported."<<std::endl;
+    //OSG_NOTICE<<"Warning: Fog::apply(State&) - not supported."<<std::endl;
 #endif
 }
diff --git a/src/osg/Light.cpp b/src/osg/Light.cpp
index f4cf9ee89..ec235698a 100644
--- a/src/osg/Light.cpp
+++ b/src/osg/Light.cpp
@@ -99,6 +99,6 @@ void Light::apply(State&) const
     glLightf ( (GLenum)((int)GL_LIGHT0 + _lightnum), GL_LINEAR_ATTENUATION,    _linear_attenuation );
     glLightf ( (GLenum)((int)GL_LIGHT0 + _lightnum), GL_QUADRATIC_ATTENUATION, _quadratic_attenuation );
 #else
-    OSG_NOTICE<<"Warning: Light::apply(State&) - not supported."<<std::endl;
+    //OSG_NOTICE<<"Warning: Light::apply(State&) - not supported."<<std::endl;
 #endif
 }
diff --git a/src/osg/PolygonMode.cpp b/src/osg/PolygonMode.cpp
index 35db6758f..76e8265f2 100644
--- a/src/osg/PolygonMode.cpp
+++ b/src/osg/PolygonMode.cpp
@@ -83,7 +83,7 @@ void PolygonMode::apply(State&) const
         #endif
     }
 #else
-    OSG_NOTICE << "Warning: PolygonMode::apply(State&) - is not supported." << std::endl;
+    //OSG_NOTICE << "Warning: PolygonMode::apply(State&) - is not supported." << std::endl;
 #endif
 }
 
diff --git a/src/osg/State.cpp b/src/osg/State.cpp
index 152decdcc..819337813 100644
--- a/src/osg/State.cpp
+++ b/src/osg/State.cpp
@@ -297,6 +297,59 @@ void State::initializeExtensionProcs()
     #endif
     }
 
+        // These fixed-function enums aren't in the GLES headers; define their classic GL values.
+        #ifndef GL_FOG
+        #define GL_FOG 0x0B60
+        #endif
+        #ifndef GL_POINT_SMOOTH
+        #define GL_POINT_SMOOTH 0x0B10
+        #endif
+        #ifndef GL_LINE_SMOOTH
+        #define GL_LINE_SMOOTH 0x0B20
+        #endif
+        #ifndef GL_COLOR_MATERIAL
+        #define GL_COLOR_MATERIAL 0x0B57
+        #endif
+        #ifndef GL_DEPTH_CLAMP
+        #define GL_DEPTH_CLAMP 0x864F
+        #endif
+        #ifndef GL_CLIP_PLANE0
+        #define GL_CLIP_PLANE0 0x3000
+        #endif
+        #ifndef GL_POLYGON_OFFSET_POINT
+        #define GL_POLYGON_OFFSET_POINT 0x2A01
+        #endif
+        #ifndef GL_POLYGON_OFFSET_LINE
+        #define GL_POLYGON_OFFSET_LINE 0x2A02
+        #endif
+        #ifndef GL_TEXTURE_2D
+        #define GL_TEXTURE_2D 0x0DE1
+        #endif
+
+        setModeValidity(GL_FOG,            false); // 0x0B60
+        setModeValidity(GL_LIGHTING,       false); // 0x0B50
+        setModeValidity(GL_NORMALIZE,      false); // 0x0BA1
+        setModeValidity(GL_RESCALE_NORMAL, false); // 0x803A
+        if (!osg::isGLExtensionSupported(_contextID,"GL_QCOM_alpha_test"))
+            setModeValidity(GL_ALPHA_TEST,     false); // 0x0BC0
+        setModeValidity(GL_POINT_SMOOTH,   false); // 0x0B10
+        setModeValidity(GL_LINE_SMOOTH,    false); // 0x0B20
+        setModeValidity(GL_COLOR_MATERIAL, false); // 0x0B57
+        if (!osg::isGLExtensionSupported(_contextID,"GL_EXT_clip_cull_distance"))
+        {
+            setModeValidity(GL_CLIP_PLANE0,    false); // 0x3000
+            OSG_NOTICE<<std::endl<<"GL_EXT_clip_cull_distance not supported."<<std::endl;
+        }
+        if (!osg::isGLExtensionSupported(_contextID,"GL_EXT_depth_clamp"))
+        {
+            setModeValidity(GL_DEPTH_CLAMP,    false); // 0x3000
+            //OSG_NOTICE<<"GL_EXT_DEPTH_CLAMP not supported. Shadows will not work."<<std::endl;
+        }
+        setModeValidity(GL_POLYGON_OFFSET_POINT,    false); // 0x2A01
+        setModeValidity(GL_POLYGON_OFFSET_LINE,    false); // 0x2A02
+
+        for (unsigned int emUnit = 0; emUnit < 16; ++emUnit)
+            getOrCreateTextureModeMap(emUnit)[GL_TEXTURE_2D].valid = false;
 
     _extensionProcsInitialized = true;
 
@@ -459,9 +459,18 @@ void State::glDrawBuffer(GLenum buffer)
 {
     if (_drawBuffer!=buffer)
     {
-        #if !defined(OSG_GLES1_AVAILABLE) && !defined(OSG_GLES2_AVAILABLE) && !defined(OSG_GLES3_AVAILABLE)
-        ::glDrawBuffer(buffer);
-        #endif
+//        #if !defined(OSG_GLES1_AVAILABLE) && !defined(OSG_GLES2_AVAILABLE) && !defined(OSG_GLES3_AVAILABLE)
+
+        if (buffer == GL_COLOR_ATTACHMENT1) {
+            GLenum buffers[2] = {GL_NONE, buffer};
+            ::glDrawBuffers(2, buffers);
+        }
+        else
+        {
+            GLenum buffers[1] = {buffer};
+            ::glDrawBuffers(1, buffers);
+        }
+//        #endif
         _drawBuffer=buffer;
     }
 }
@@ -470,9 +524,9 @@ void State::glReadBuffer(GLenum buffer)
 {
     if (_readBuffer!=buffer)
     {
-        #if !defined(OSG_GLES1_AVAILABLE) && !defined(OSG_GLES2_AVAILABLE) && !defined(OSG_GLES3_AVAILABLE)
+//        #if !defined(OSG_GLES1_AVAILABLE) && !defined(OSG_GLES2_AVAILABLE) && !defined(OSG_GLES3_AVAILABLE)
         ::glReadBuffer(buffer);
-        #endif
+//        #endif
         _readBuffer=buffer;
     }
 }
diff --git a/src/osgUtil/RenderStage.cpp b/src/osgUtil/RenderStage.cpp
index f0d23158f..1e47b2cdd 100644
--- a/src/osgUtil/RenderStage.cpp
+++ b/src/osgUtil/RenderStage.cpp
@@ -517,10 +517,10 @@ void RenderStage::runCameraSetUp(osg::RenderInfo& renderInfo)
             // and before call to glCheckFramebufferStatus
             if ( !colorAttached )
             {
-            #if !defined(OSG_GLES1_AVAILABLE) && !defined(OSG_GLES2_AVAILABLE) && !defined(OSG_GLES3_AVAILABLE)
+//            #if !defined(OSG_GLES1_AVAILABLE) && !defined(OSG_GLES2_AVAILABLE) && !defined(OSG_GLES3_AVAILABLE)
                 setDrawBuffer( GL_NONE, true );
                 state.glDrawBuffer( GL_NONE );
-            #endif
+//            #endif
             }
 
             GLenum status = ext->glCheckFramebufferStatus(GL_FRAMEBUFFER_EXT);
@@ -808,9 +808,9 @@ void RenderStage::copyTexture(osg::RenderInfo& renderInfo)
 
     if ( _readBufferApplyMask )
     {
-        #if !defined(OSG_GLES1_AVAILABLE) && !defined(OSG_GLES2_AVAILABLE)
+//        #if !defined(OSG_GLES1_AVAILABLE) && !defined(OSG_GLES2_AVAILABLE)
             glReadBuffer(_readBuffer);
-        #endif
+//        #endif
     }
 
     // need to implement texture cube map etc...
@@ -909,7 +909,7 @@ void RenderStage::drawInner(osg::RenderInfo& renderInfo,RenderLeaf*& previous, b
 
     if (!using_multiple_render_targets)
     {
-        #if !defined(OSG_GLES1_AVAILABLE) && !defined(OSG_GLES2_AVAILABLE) && !defined(OSG_GLES3_AVAILABLE)
+//        #if !defined(OSG_GLES1_AVAILABLE) && !defined(OSG_GLES2_AVAILABLE) && !defined(OSG_GLES3_AVAILABLE)
 
             if( getDrawBufferApplyMask() )
                 state.glDrawBuffer(_drawBuffer);
@@ -917,7 +917,7 @@ void RenderStage::drawInner(osg::RenderInfo& renderInfo,RenderLeaf*& previous, b
             if( getReadBufferApplyMask() )
                 state.glReadBuffer(_readBuffer);
 
-        #endif
+//        #endif
     }
 
     if (fbo_supported)
@@ -995,7 +995,7 @@ void RenderStage::drawInner(osg::RenderInfo& renderInfo,RenderLeaf*& previous, b
                 blitMask, GL_NEAREST);
         }
 
-#if !defined(OSG_GLES1_AVAILABLE) && !defined(OSG_GLES2_AVAILABLE) && !defined(OSG_GLES3_AVAILABLE)
+//#if !defined(OSG_GLES1_AVAILABLE) && !defined(OSG_GLES2_AVAILABLE) && !defined(OSG_GLES3_AVAILABLE)
         if (needToBlitColorBuffers)
         {
             for (FrameBufferObject::AttachmentMap::const_iterator
@@ -1021,7 +1021,7 @@ void RenderStage::drawInner(osg::RenderInfo& renderInfo,RenderLeaf*& previous, b
             // glReadBuffer(GL_COLOR_ATTACHMENT0_EXT);
             // glDrawBuffer(GL_COLOR_ATTACHMENT0_EXT);
         }
-#endif
+//#endif
 
         apply_read_fbo = true;
         read_fbo = _resolveFbo.get();
@@ -1044,7 +1044,7 @@ void RenderStage::drawInner(osg::RenderInfo& renderInfo,RenderLeaf*& previous, b
         {
             if (read_fbo) SubFunc::applyReadFBO(apply_read_fbo, read_fbo, state);
 
-            #if !defined(OSG_GLES1_AVAILABLE) && !defined(OSG_GLES2_AVAILABLE)
+//            #if !defined(OSG_GLES1_AVAILABLE) && !defined(OSG_GLES2_AVAILABLE)
 
                 if (using_multiple_render_targets)
                 {
@@ -1062,7 +1062,7 @@ void RenderStage::drawInner(osg::RenderInfo& renderInfo,RenderLeaf*& previous, b
                     }
                 }
 
-            #endif
+//            #endif
 
             GLenum pixelFormat = itr->second._image->getPixelFormat();
             if (pixelFormat==0) pixelFormat = _imageReadPixelFormat;
